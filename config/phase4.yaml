# Module 4: RL Fine-Tuning (PURE + ReCode GRPO)

rl:
  group_size: 4
  learning_rate: 1.0e-5
  kl_coeff: 0.04
  pure_min_form: true
  execution_gate: true

data:
  input_path: data/processed/cocte/cocte_labeled.jsonl
  output_dir: data/processed/rl

sandbox:
  database_root: data/sandbox
  dialect: sqlite

reward:
  process_weight: 1.0
  outcome_weight: 1.0
